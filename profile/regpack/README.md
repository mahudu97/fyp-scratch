Each of `pyfr` and `synth` contains dir with groups of matrices.

Each subdir of `pyfr` and `synth` represent a group to be profiled with VTune.

The groups were identified via the roofline plots of RegPack.
